{
  "name": "sd-flash-sale",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "load-test": "npm run -w @workspace/load-test load-test --",
    "postinstall": "bash scripts/check-deps.sh",
    "setup": "npm run -w @workspace/database db:setup && npm run -w @workspace/redis redis:setup && npm run -w @workspace/queue rabbitmq:up",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "oxfmt": "^0.65.0",
    "turbo": "^2.5.6"
  },
  "packageManager": "npm@11.9.0"
}
